import {Detail, details} from './details'

class Task {
    constructor(taskName, dueDate, ) {
        this.taskName = taskName;
        this.dueDate = dueDate;
    }
    removeTask() {
    //find this task from tasks array;
    }
}

export {Task};